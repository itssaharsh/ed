# Core
requests==2.32.4
Pillow==11.3.0
numpy>=1.26

# ffmpeg as a Python package - ships a static binary, so no apt/sudo needed
imageio-ffmpeg>=0.5.1

# LLM (google-generativeai is dead: support ended 2025-08-31, and its old model
# ids now 404. Only google-genai.)
google-genai>=1.0.0

# TTS fallback when GROQ_API_KEY is absent
edge-tts==7.2.8

# YouTube upload
google-api-python-client==2.167.0
google-auth==2.40.3
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.2
