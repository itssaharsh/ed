# Unit: stages — correctness of the writing/visual/timing stages

Owns: `run.py`, `batch.py`, `shorts/write.py`, `shorts/tournament.py`,
`shorts/visuals.py`, `shorts/store.py`, `shorts/brief.py`, `shorts/prompts.py`,
`prompts/*.md`, `briefs/*.json`, new `tests/test_stages_*.py`.

1. Read `CLAUDE.md`, `docs/ARCHITECTURE.md`, `prompts/README.md`, then every
   owned file. Trace one real run through the checkpoints by running
   `tests/run_offline.py` and reading `work/offline-*/` (01_premises → 12_meta).
2. Audit against the promises: premises generated by volume and chosen by
   PAIRWISE Swiss tournament (never a score), judges rendered with `VOICE=""`,
   six alt punchlines + tournament, designed silences (punchline 400–650 ms
   pre-beat, hook 0), premise dedup via `store.py` (`SIMILARITY_THRESHOLD`),
   brief validation rules in `briefs/README.md`, both paths converging in
   `run._finish`. For each promise: find the code that keeps it or the place
   it is broken; list crash paths (empty tournament, all premises deduped,
   a brief with fewer shots than lines, `rng` determinism under `--seed`).
3. Fix every real defect in owned files. Each fix gets a test in
   `tests/test_stages_*.py` that fails on the old code (mutation-check).
   Do not touch the calibrated constants without evidence.
4. `batch.py`: verify the serial-only design and exit-code semantics; make
   `--only`/`--limit` and the summary robust (a brief that crashes must not
   kill the batch).
5. Run `tests/test_units.py`, `tests/run_offline.py`, and the real brief
   render `run.py --brief briefs/fridge_baseline.json --dry-run --seed 7`
   (needs network; if blocked, say so). Paste counts/outputs.
6. Handoffs for files you do not own go in the results file.

End with `FLEET-RESULT`.
